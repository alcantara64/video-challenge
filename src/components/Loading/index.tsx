import { ReactComponent as Loader } from '../../images/loader.svg';

import './index.css';

const Loading = () => (
  <div id="app-loader">
    <Loader />
  </div>
);

export default Loading;
